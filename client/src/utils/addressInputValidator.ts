import { inputValideResult } from "../types/inputValidResult";

export default function addressInputValidator(addressValue:string ):inputValideResult{

    return {validStatus:'input success', validMessage:'Адресс корректный'}

}